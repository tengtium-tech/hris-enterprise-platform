{
  "_comment": "Development-environment overrides only -- non-sensitive, per docs/08-devops/environment-strategy.md. Development uses synthetic data only (never a copy of real tenant data); the database connection string itself is read from configuration/secrets at runtime, not hardcoded here.",
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
